; cat -- печатать данные, поданные на вход симулятору через файл ввода

(setq char (get))
(loop (not (= EOF char))    ; EOF == \0
    (put char)              ; put = print char, get = read char
    (setq char (get))
)
